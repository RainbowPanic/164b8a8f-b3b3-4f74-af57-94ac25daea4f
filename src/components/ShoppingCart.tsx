import * as React from 'react';
type SCProps = {
    addedEvents: any
}
export const ShoppingCart = (props: SCProps) => {

    return (
        <div>
            <p><h2>Shoppingcart</h2></p>
        </div>
    );
}